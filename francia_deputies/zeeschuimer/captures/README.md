# Capturas Zeeschuimer (solo en tu máquina)

Los archivos `*.ndjson` **no deben subirse a GitHub** (límite 100 MB por archivo).

1. Coloca aquí los export de Zeeschuimer.
2. Procesa desde `francia_deputies/`:

   `python3 zeeschuimer/scripts/merge_zeeschuimer_with_deputies.py`

Los resultados útiles para el repo suelen ser los CSV en `zeeschuimer/processed/`.
